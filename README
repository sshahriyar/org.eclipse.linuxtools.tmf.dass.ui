Detective is an intrusion detection software in Java and available as an eclipse  plugin.
It can detect anomalous behaviour of software systems by using only nomral behaviour and by using both normal and historical anomalous behaviour.
It is both an ADS and IDS for a host; i.e., a host based anomalous beahviour detector. 
